For easy use of making coverage in this folder of repository. Use this commands:

---

Only if you don't yet install dependencies.

`npm install`

---

Run coverage generator:

`npm run coverage`

---

And to see that, use next command:

`npm run see-coverage`