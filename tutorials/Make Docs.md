For easy use of making docs in this folder of repository. Use this commands:

---

Only if you don't yet install dependencies.

`npm install`

---

Run docs generator:

`npm run docs`

---

And to see that, use next command:

`npm run see-docs`