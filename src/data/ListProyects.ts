import { LIST_PROJECTS } from './siteData';
/**
 * Lista de proyectos para mostrar en la sección de proyectos.
 * Los datos se definen en src/data/siteData.ts para facilitar la edición.
 * @const
 * @example
 * import ListProyects from 'data/ListProyects'
 * ListProyects[0].name
 */
const ListProyects = LIST_PROJECTS;

export default ListProyects;
